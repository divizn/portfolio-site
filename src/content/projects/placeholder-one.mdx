---
title: "Placeholder Project One"
description: "PLACEHOLDER — replace with real project copy during the M6 content pass."
date: 2026-01-01
published: true
featured: true
repository: "divizn/placeholder-one"
tags: ["typescript", "astro"]
---

This is placeholder body content used to verify the MDX rendering pipeline
(headings, code blocks, links) end-to-end. It will be replaced with a real
project write-up during the content pass.

## Example heading

Verifies `rehype-slug` + `rehype-autolink-headings` produce an anchor link.

```ts
export function example(): string {
  return "shiki syntax highlighting check";
}
```

A [link](https://example.com) to verify inline MDX links render correctly.
